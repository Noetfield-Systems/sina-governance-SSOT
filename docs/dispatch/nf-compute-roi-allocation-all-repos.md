# Dispatch — compute / ROI allocation (all repos)

**Authority:** `SG-Canonical-Library/noetfield-library/P0-FOUNDATION-SPINE/NF_COMPUTE_ROI_ALLOCATION_LOCKED_v1.md`  
**Machine:** `data/nf_compute_roi_allocation_v1_LOCKED.json`  
**decision_id:** `NF-COMPUTE-ROI-ALLOCATION-V1`  
**Status:** `SG_ACCEPTED` · `IMPLEMENTATION_AUTHORIZED`  
**Pointer:** `data/sg-authority-ref-compute-roi-v1.json`  
**PR:** https://github.com/Noetfield-Systems/sina-governance-SSOT/pull/62

## One-liner (paste into any agent session)

```text
Compute ROI law: enterprise plan pays governance minutes; Cloudflare Workers + Workflows and Railway run scale and margin; economical LLM matrix is Kernel policy. Actions minutes = ROI classes A–D against 50k/month — not 24/7 product schedule. Job wake = authenticated HTTP job_id only. HOLD preserved. Cross-repo: install noetfield.sg-authority-ref.v1 pointer only; do not duplicate the lock.
```

## Per-repo

| Repo | Agent / lane | Action |
|------|--------------|--------|
| sina-governance-SSOT | sg_sssot_cursor | Canon + machine JSON + read surfaces + this dispatch |
| noetfeld-OS | noos_agent | Install `data/sg-authority-ref-compute-roi-v1.json`; observe GHA burn; no redefine |
| SourceA | sourcea_brain / worker | Install pointer; Brain/Job compile must not invent Actions cron muscle |
| NOETFIELD-RUNWAY (+ builder clones) | runway builders | Install pointer; event-driven job_id wake; empty product crons; Railway for heavy only |
| trustfield-loops / TrustField-Technologies | trustfield_worker | Consume pointer; no product schedule on GHA |
| Noetfield website | noetfield_website | No Actions-as-runtime; publish jobs only when commissioned |

## Forbidden

- Cancel the enterprise plan without founder order
- `schedule` / continuous `repository_dispatch` as product muscle
- Feature-branch CI spam against the 50k minute budget
- idle llm ticks / health-model cron
- Duplicating full lock text into venture repos
- Lifting `AUTONOMOUS_PRODUCTION_MUTATIONS=HOLD`
