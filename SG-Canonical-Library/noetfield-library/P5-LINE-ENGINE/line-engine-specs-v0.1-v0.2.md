# LINE ENGINE — FULL SPECS v0.1 + v0.2 HARDENED (pointer + summary)

**Status:** Full specs authored this chat. Files: `LINE_ENGINE_ARCHITECTURE_v0.1.md`, `LINE_ENGINE_ARCHITECTURE_v0.2_HARDENED.md` (in /outputs; copy into repo alongside library).
**First written:** 2026-07-03 12:34 PDT

## v0.1 — skeleton
Build the full-stack Line Engine ONCE (plan→produce→verify→ship→measure), instantiate per line (config + offer + surface + verify profile). Three layers: Hub (founder, tap-only) / Line Engine ×N / Substrate (the floor). ROI ranks lines.

## v0.2 HARDENED — the 24/7 organism
Adds: operating gears (OBSERVE→CONFIRM→SEMI→AUTO-24/7→HEAL-ONLY, earned per line); self-heal loop (bounded, metric-or-revert); research station (Perplexity/computer-use — verifies claims by exercising the surface); AI stations (adversarial roles, same-base-model convergence=warning); kill-switch + per-line freeze + blast-radius + anomaly→degrade-toward-safety; ROI engine (compute follows money, no line raises its own budget); Live Brain control plane (reads truth, routes, can't edit floor); immutable floor (loop can't weaken its own brakes).

## The governing law
A self-improving system may never improve away its own brakes. Determinism + immutable floor = safe 24/7 autonomy. Test the kill-switch BEFORE anything ships unattended.

## Relation to Cloud Kernel v1.3
The Line Engine's plan→produce→verify→ship→measure = the kernel's PLAN→CONTROL→EXECUTE→VERIFY→COMMIT (5-stage-loop.md), nested under L0's PROVE gate. Execution Contract = the verify layer's brain. Blueprint Registry = per-line versioned contracts.

---
*v0.1 (2026-07-03 12:34 PDT) — pointer + summary; full specs in /outputs (v0.1 skeleton, v0.2 hardened organism).*
