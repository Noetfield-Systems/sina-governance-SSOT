# NF-COMPANY-NEW-PRODUCT-V1 — SG LOCK

**decision_id:** `NF-COMPANY-NEW-PRODUCT-V1`  
**Status:** `SG_ACCEPTED` · `IMPLEMENTATION_AUTHORIZED`  
**Tier:** P10-PRODUCT-LAYERS  
**Version:** `v1.0.0_locked_20260720`  
**Machine:** `data/nf_company_new_product_v1_LOCKED.json`  
**Product surface:** Cloudflare Pages standalone site `sites/company-new` (RUNWAY) — public path `/new`  
**Benchmark (UX only):** [polsia.com/new](https://polsia.com/new) — Polsia is the product-class reference; Noetfield ships the deterministic runtime underneath  
**Depends on:** `NF-NOETFIELD-RUNWAY-PRODUCT-V1` · `NF-UNIFIED-MOTOR-ARCHITECTURE-V1` · `NF-COMPUTE-ROI-ALLOCATION-V1` · `NF-WAKE-PATH-CONFLICT-POLICY-V1` · `NF-DETERMINISTIC-API-PRODUCT-AND-GTM-V1`  
**Does not activate:** `AUTONOMOUS_PRODUCTION_MUTATIONS=RUN` · unsupervised marketing/sales/support company loops · frontier-model-as-default · Agent Swarm SKU

---

## One-line law

Polsia-class `/new` is the UX benchmark; Noetfield delivers **idea → verified staging** via recipes, T0-first Motor dispatch, and deterministic acceptance — not open-ended co-founder chat.

---

## Distinction (binding)

```text
Polsia public UX (plans / codes / markets / operates)
≠ Noetfield Company New product (idea → staging with receipts)
≠ Continuous company operation (deferred after staging loop is reliable)
```

Architecture accepted ≠ implemented ≠ deployed ≠ proven ≠ commissioned ≠ ACTIVE for unsupervised production mutation. HOLD preserved.

---

## Product definition

| Field | Value |
|-------|-------|
| product | Noetfield Company New |
| buyer | Founder / operator who wants a real staging app from a spec, not a chat transcript |
| input | Project idea + recipe selection + bounded spec |
| result | Verified staging URL + acceptance evidence + `job_id` receipt |
| public_entry | `/new` on standalone CF Pages (`nf-company-new`) |
| flow | Spec → Deterministic contract + plan → Motor / SG / Runway gates → Dispatcher T0–T3 → Build / test / bounded repair → Provision → Staging → Evidence → STOP |
| time_to_result | Minutes to a few hours by recipe |
| pricing | Credits per completed Job (Runway law); not per chat token |
| human | T3 only for irreversible risk (money, secrets, prod promote, legal) |

---

## Fast + high quality + cheap (binding knobs)

| Goal | Binding method |
|------|----------------|
| Cheap | Maximize **T0** (templates, scaffolds, tools — no LLM). Escalate only with receipt. LLM matrix: DeepSeek default → GLM repair → Kimi long-context. Compress prompts. Semantic cache when available. |
| Fast | Pre-baked **recipes**. Event wake by authenticated `job_id`. Parallel isolated worktrees. Cloudflare hot path; Railway heavy only. Cap repair rounds and token budget per job. |
| High quality | Acceptance **contract object** before LLM: tests, smoke URLs, schema gates, HOLD on money/secrets/prod. Strong model only for hard diffs. |

---

## Dispatcher tiers (binding)

| Tier | Use |
|------|-----|
| **T0** | Code/tool/scaffold only — no LLM |
| **T1** | Cheap/fast model (fill copy gaps, classify, light repair) |
| **T2** | Strong model for difficult architecture or multi-file repair |
| **T3** | Human approval for critical risk |

Retry ladder: syntax → T0; test fail → T1; architecture → T2; missing credential / ambiguous business → founder packet (block).

---

## Qualification recipes (v1 — all required)

First milestone ships **all three** recipe paths as selectable products on `/new`. Anything outside recipes → T3 / founder packet.

1. **Marketing site** — CF Pages/Workers landing + contact form + smoke 200s  
2. **Deterministic API** — OpenAI-compatible `POST /v1/chat/completions` plane (reuse det-api DNA; buyer language only)  
3. **CRUD SaaS** — auth + one table + one UI + staging health

**v1 success:** submit one idea through `/new` → runtime delivers a **verified staging application** using Noetfield Cloudflare, GitHub Actions (ROI classes only), workers, models, policies, and databases.

**Explicitly deferred:** marketing automation, sales, support, and continuous “company while you sleep” operation — after staging loop is boringly reliable.

---

## Pipeline (binding)

```text
Project specification
→ Deterministic contract + plan
→ New repository + isolated sandbox / worktrees
→ Kernel and Motor
→ SG / Runway policy gates
→ Planner and role assignment
→ Model + machine dispatch (T0–T3)
→ Build, test, bounded repair
→ Infrastructure provisioning
→ Staging deployment
→ Evidence and acceptance
→ STOP (continued scheduled operation = later passport-gated loops)
```

Wake default: authenticated HTTP `job_id` (compute ROI + wake-path policy). Schedules allowed only with schedule passport.

---

## Buyer language (binding)

Public `/new` surface may say: project, company start, recipe, staging, verified.  
Must **not** sell Motor / Runway / NOOS / SG internals as the buyer SKU.  
Must **not** claim unsupervised production promote while HOLD is active.

---

## Standalone surface (binding)

| Field | Value |
|-------|-------|
| repo | `Noetfield-Systems/NOETFIELD-RUNWAY` |
| path | `sites/company-new/` |
| host | Cloudflare Pages project `nf-company-new` |
| routes | `/` hub · `/new` create flow · `/catalog.json` machine catalog · `/healthz` |
| lock_doc | this packet |

Sister sites may link to `/new`; they must not fork a parallel Polsia-class SKU with different acceptance law.

---

## Forbidden

- Inventing `MOTOR_PRODUCTION_PATH_BLOCKED` solely because a passported schedule exists  
- Frontier/default LLM for scaffoldable work  
- Open-ended multi-agent debate as the motor  
- Claiming continuous company operation in v1 marketing copy  
- Lifting HOLD from this product lock alone
