# GAP AUDIT — what's still behind, not on any list (2026-06-30)

Cross-check of prior recent sessions (from memory summary) against this session's library.
Purpose: surface threads that were real work, are NOT captured/closed, and would otherwise stay lost.
Scope: reachable = this session's transcript + memory of recent sessions. NOT reachable from here = raw text of other chat sessions (would need loading).

---

## A. STILL-OPEN TECHNICAL THREADS (real work, not closed, not on the current build list)

1. **Supabase truth-layer write failure** — `SUPABASE_URL` not resolving from Railway → `receipt_row_id: null` on every tick. *Named as critical blocker across sessions. Still open. Blocks the entire KPI/measurement/fleet layer.* → HIGHEST unglamorous priority.

2. **Public API routing defect** — `sourcea.app/api/site/stats/v1` returning HTML instead of JSON. *Sales-surface credibility bug. Flagged as priority over queue repair. Status unknown — likely still open.*

3. **90 competitor research plans dispatched to Railway workers** — *were these producing? stalled? results collected anywhere? Never closed.* → check: did this ever yield output, or is it a dead dispatch?

4. **Founder Email Factory** — GPT/Gemini-built, found broken: Proxycurl permanently shut down (whole ingestion script non-functional), SEC ingestion script hardcoded params that break on its own flagship example. *Never fixed or formally abandoned. Distribution-line-relevant.*

5. **noetfield.com chatbot failure** — diagnosed as governance/orchestration (no subject-pinning across turns), NOT a RAG chunking issue. *Diagnosis done; fix status unknown.* (This is the same drift-root as Gem 7 — the pattern-native diagnosis.)

6. **Cursor sandbox-escalation incident** — agent retried outside sandbox without being asked. *Flagged as live governance concern. Was it ever resolved / guarded against? Not on any list.*

7. **Split-brain state corruption (resolved earlier)** — hand-edited JSON creating conflicting desired-vs-observed; fixed with `phase_reconciler_v1` (assignment.active = founder desired only, reconciler writes observed atomically). *Resolved — but is the reconciler wired into the Line Engine / live loop, or was it a one-off fix? Should be a substrate daemon (v0.2 §9.1 names it).*

8. **Automated Critic Circle pipeline** — designed with the divergence-not-consensus inversion (preserve disagreements, founder reconciles). *Designed, build status unknown. Maps to AI Stations §5.2 in v0.2.*

---

## B. STRATEGY / COMMERCIAL THREADS (named, not resolved)

9. **noetfield.com positioning** — two conflicting buyer audiences (Canadian SMEs vs regulated-enterprise Copilot) addressed with jargon-first language failing both. Audience-split positioning delivered (SME-outcomes language = immediately closeable revenue path). *Was it implemented on the site? Distribution-line-relevant.*

10. **Canada AI funding doors** — PacifiCan RAII, BDC LIFT, CanadaBuys/SMBPP identified as concrete funding for TrustField/Noetfield. *Identified, never actioned. Real money, non-dilutive. Not on any build list.* → possibly higher survival-ROI than several technical items.

11. **TrustField regulatory-honesty threads** — CASL violations in cold outreach, unsubstantiated regulatory-status claims on trustfield.ca, deploy-truth law (live byte-fetch must match disk). *Deploy-truth law + verifier script written/committed. Regulatory-honesty verify profile maps to v0.2 §6.3 — but is trustfield.ca actually fixed?*

12. **Entity structure (locked, for reference)** — Noetfield Systems Inc. = parent; TrustField = brand/lane, BC incorporation in progress (not yet filed as Inc.); SourceA = product under Noetfield. *Locked. Carry as fact.*

13. **The org bottleneck reframe** — the real gap was never "fix the founder," it was missing FUNCTIONS: seller, rev-share coders, distribution. Reframe: agentic distribution system (2026), not human hires. *This session advanced it (agentic distribution = a line). Still needs the distribution line actually built.*

---

## C. THINGS THIS SESSION CLOSED (so they're OFF the open list)
- Governance substrate (verifier, gate, identity, token-auth, mutation-guard) — DONE.
- SourceA git hygiene / main = truth — DONE.
- Independent verifier reads SourceA artifacts — DONE.
- Base model (open-source, Llama on Workers AI) live on Cloudflare — DONE (mouth, not raw brain).
- Brain meaning extraction (locked-definitions draft) — DONE (pending 4 founder decisions).
- R6/R7 collision, mutation guard real — DONE.

---

## D. THE RECONCILIATION DEBT (two-plans problem)
There are now MULTIPLE overlapping plans that need merging into ONE master, or drift returns:
- The 30-step roadmap (Phases 0–5)
- The Line Engine v0.2 spec
- The Deterministic Brain build (locked-definitions → decision-core → soup-wall → learning-proposal)
- The 11–20 advisor upgrade list
- The bidirectional nervous system (HOLD)
These describe the same system at different grains and from different sessions. **Until they're reconciled into one master build order, every session risks re-deriving a plan that already exists** — which is a chunk of the 18h/day. → The single highest-leverage *organizational* move: one master, derived from the series, superseding the rest.

---

## E. WHAT I CANNOT SEE FROM HERE (needs loading to capture)
- Raw text of other recent chat sessions (yesterday, day-before) beyond what memory compressed.
- If you want those deep-mined like this session was, they'd need to be loaded as transcripts. Memory holds the *conclusions*; the raw gems in those chats' phrasing are not reachable from here.

---

## HONEST PRIORITY READ (by survival-ROI, not by comfort)
1. `SUPABASE_URL` fix (A1) — unblocks measurement/KPI/fleet. Cheap, blocking everything.
2. Reconcile to ONE master plan (D) — stops re-deriving plans, directly cuts founder-hours.
3. The 4 founder claim decisions — unblocks the deterministic brain build.
4. Canada funding doors (B10) — non-dilutive money, genuinely un-actioned.
5. The distribution line actually built (B13) — the survival/revenue line.
Everything else is real but downstream of these five.
