# BRAIN MEANING — LOCKED-DEFINITIONS v1 (the brain's meaning source)

**Status:** DRAFT — extracted + provenance-tagged; 4 founder decisions OPEN before live_lock. Owner: SourceA Brain Agent.
**First written:** 2026-07-03 12:34 PDT

## What it is
The single meaning source the deterministic decision-core reads. Extract-then-approve (NOT blank-page): meaning extracted from live materials, provenance-tagged (source_path + literal text), founder ratifies. Public models never own meaning — it lives here, in the core, fixed.

## Build state
- Draft extracted from live materials (brain-public-rules, knowledge bundle, site copy, Forge routes, proof pages, live claims, forbidden-public rule, live-status-first rule, demo/CTA rule).
- v6 framing sourced from governance disk.
- Items tagged: approved_existing (ratified draft) / needs_founder_review / unsafe_or_unclear.

## THE 4 OPEN FOUNDER DECISIONS (gate → live_locked)
1. **sourcea_is_live** — define the canonical liveness signal (which endpoint/response = "SourceA is live").
2. **forge_terminal_guaranteed_live_runtime** — do you stand behind "Forge live & usable now"? by what signal? (else → requires_status_signal → graceful gear when unconfirmed).
3. **every_possible_run_has_public_proof** — narrow the overclaim to "designed around verifiable receipts" (true/defensible).
4. **broken_gears** — author the 3-gear ladder (verified→confident; unsure→entice+escalate; broken→honest redirect). Warmth always, confidence tracks signal.

## Structure (target shape)
terms{sourcea,forge} · claims[{id,approved_text,requires_status_signal,status_key}] · forbidden_public[...] · language_split{public,private} · response_ladders{unsure,broken}.

## Downstream build (after ratify)
decision-core-v1 (deterministic Python over this file) → soup-wall-v1 (Llama drafts the decided answer + sanitizer) → learning-proposal-v1 (gated definition-updates). On the Llama-mouth deploy + substrate.

## Base-model note
Per base-model-first-class-language: this file IS the model's language layer, loaded before any other asset. Not config — language. LOCKED items = word definitions (changing one changes what the model means downstream).

---
*v0.1 (2026-07-03 12:34 PDT) — first write. Locked-definitions draft state + 4 open founder decisions + structure + downstream build. From this chat.*
